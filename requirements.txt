# --- Web framework ---
fastapi==0.115.0
uvicorn[standard]==0.30.6

# --- Database / ORM ---
SQLAlchemy==2.0.35
# Postgres driver. `postgresql://` URLs (as in .env) default to psycopg2 in
# SQLAlchemy; psycopg2-binary bundles libpq so no system dependency is needed.
psycopg2-binary==2.9.10

# --- Validation ---
pydantic==2.9.2
email-validator==2.2.0

# --- Auth ---
python-jose[cryptography]==3.3.0
passlib==1.7.4
# bcrypt is pinned to 4.0.1 because passlib 1.7.4 reads bcrypt's removed
# __about__ module in 4.1+, which raises an AttributeError at runtime.
bcrypt==4.0.1
# Required by FastAPI's OAuth2PasswordRequestForm (form parsing).
python-multipart==0.0.9

# --- Config / env ---
python-dotenv==1.0.1

# --- Testing ---
pytest==8.3.3
httpx==0.27.2